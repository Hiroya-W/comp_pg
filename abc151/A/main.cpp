#include <bits/stdc++.h>
using namespace std;

#define EPS (1e-7)
#define INF (1e9)
#define PI (acos(-1))

#define rep(i, n) for (int i = 0; i < (int)(n); i++)

typedef long long ll;

void solve(std::string C)
{
    char c = C[0];
    cout << (char)(c + 1) << endl;
}

int main()
{
    std::string C;
    std::cin >> C;
    solve(C);
    return 0;
}
