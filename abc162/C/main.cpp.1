#include <bits/stdc++.h>
using namespace std;

#define EPS (1e-7)
#define INF (1e9)
#define PI (acos(-1))
#define deg_to_rad(deg) (((deg) / 360) * 2 * M_PI)
#define rad_to_deg(rad) (((rad) / 2 / M_PI) * 360)

#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep1(i, n) for (int i = 1; i < (int)(n); i++)

typedef long long ll;

void solve(long long K)
{
    ll ans = 0;
    rep1(i, K + 1)
    {
        rep1(j, K + 1)
        {
            rep1(k, K + 1) { ans += gcd(i, gcd(j, k)); }
        }
    }
    cout << ans << endl;
}

int main()
{
    long long K;
    scanf("%lld", &K);
    solve(K);
    return 0;
}
